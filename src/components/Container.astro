---
import type { ContainerProps as Props } from "~/types";
const { class: className = "", contain = true } = Astro.props;
const classes = `${className} ${contain ? "container py-16 lg:py-24" : ""}`;
---

<div class={classes}>
	<slot />
</div>
